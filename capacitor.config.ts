import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apd.dashboard',
  appName: 'APD | Analytic Personal Dashboard',
  webDir: 'build'
};

export default config;
